#include<bits/stdc++.h>
#include<fstream>
using namespace std;
int main()
{
    ifstream fi;
    fi.open  ("intput.cpp", ios_base::in);
    if(fi==NULL)
    {
        cout<<"Khong mo duoc file!";	
    }
    else
    {
        cout<<"Mo duoc file!";
        int x;
        fi>>x;
        while(eof.size())
        {
        	int max=0;
        	if(x>max)
        	{
        		max=x;
			}
		} cout<<max;
    }
}

#include<bits/stdc++.h>
using namespace std;

int Intput(int *a, int n)
{
	for(int i=0; i<n; i++)
	{
		cin>>a[i];
	}
}
int Output(int *a, int n)
{
	for(int i=0; i<n; i++)
	{
		cout<<a[i]<<" ";
	}
}
int Dem(int *a, int n)
{
	for()
}
int main()
{
	int n; cin>>n;
	int *a=new int[n];
	Intput(a,n);
	Output(a,n);
}
